These are scripts intended to patch OMNeT++ and INET to allow RayNet to function.
- These are likely no longer needed after the update to the OMNeT++ 6.1, but this needs to be tested
- If omnet starts throwing errors about NED files already being loaded, try to run this patch