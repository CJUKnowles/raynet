# Things to do
- [ ] Figure out how to add OmnetBind to the virtual environment (no more symbolic links or manually finding the module in code)
- [ ] Finish omnetpp6.3 port (fix the reset segfaults)
- [ ] Update cartpole example to work with current architecture (mostly just add explicit step scheduling)
- [ ] Figure out how load the official Orca model from a checkpoint and do inference
- [ ] Add a non-paced CC example that doesn't require custom version of INET for new users
- [ ] Integrate RayNet with QTEnv and the IDE (may already be possible?)
- [ ] Fix Astraea. Currently works but does not improve in training.
- [ ] Add multi-agent support to Orca (assign unique RLID's like Astraea, and don't extract raw obs/reward/action values in Orca's training/eval scripts)
- [ ] Figure out a multi-RL-protocol workflow for competition experiments
- [ ] Make exp runner and exp plotter scripts more modular and less spaghetti
- [ ] Force vecs to always output (even for short simulations or at the tail end of simulations)
- [ ] Remove all Cmdrlenv outputs when in silent mode
- [ ] Add experiment fault-tolerance to allow experiments to continue running if one fails
- [ ] Track experiment runner progress so it can easily be paused and resumed
- [ ] Add experiment runner/plotter progress outputs and time estimates
- [ ] Reduce the number of vectors recorded for experiments (could only get them to work reliably by outputting all of them)
- [ ] Update python libraries where possible. Hunt down some of those startup warnings. 
- [ ] Clean up build scripts and remove their deprecated features (like passing CLEAN_ALL to cmake)
- [ ] Prune requirements-extra.txt of any unused modules. Anything not currently used in training scripts or experiment_runner/plotter are probably useless.
- [ ] 