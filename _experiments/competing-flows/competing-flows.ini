# Two-minute run, a single isolated flow sending data to a server.

[General]
ned-path = HOME/raynet/_topologies:HOME/inet4.5/examples:HOME/inet4.5/showcases:HOME/inet4.5/src:HOME/inet4.5/tests/validation:HOME/inet4.5/tests/networks:HOME/inet4.5/tutorials:HOME/omnetpp/samples/tcpPaced/src:HOME/omnetpp/samples/tcpGoodputApplications/src:HOME/omnetpp/samples/cubic/src:HOME/raynet/simlibs/RLComponents/src:HOME/raynet/simlibs/Orca/src:HOME/raynet/simlibs/Astraea/src:HOME/raynet/simlibs/CleanSlate/src:
# General
sim-time-limit = 40s

record-eventlog=false
cmdenv-express-mode = true
cmdenv-redirect-output = false
cmdenv-output-file = dctcpLog.txt
**.client*.tcp.conn-8.cmdenv-log-level = detail
cmdenv-log-prefix = %t | %m |

cmdenv-event-banners = false
**.cmdenv-log-level = off

**.**.tcp.conn-*.cwnd:vector(removeRepeats).vector-recording = true
**.**.tcp.conn-*.rtt:vector(removeRepeats).vector-recording = true
**.**.tcp.conn-*.srtt:vector(removeRepeats).vector-recording = true
**.**.tcp.conn-*.throughput:vector(removeRepeats).vector-recording = true
**.**.tcp.conn-*.retransmissionRate:vector(removeRepeats).vector-recording = true
**.**.tcp.conn-*.**.result-recording-modes = vector(removeRepeats)

**.**.queue.queueLength:vector(removeRepeats).vector-recording = true
**.**.queue.queueLength.result-recording-modes = vector(removeRepeats)

**.**.goodput:vector(removeRepeats).vector-recording = true
**.**.goodput.result-recording-modes = vector(removeRepeats)

**.**.bandwidth:vector(removeRepeats).vector-recording = true
**.**.bandwidth.result-recording-modes = vector(removeRepeats)

**.**.mbytesInFlight:vector(removeRepeats).vector-recording = true
**.**.mbytesInFlight.result-recording-modes = vector(removeRepeats)

**.scalar-recording=false
**.vector-recording=false
**.bin-recording=false

**.goodputInterval = 1s
**.throughputInterval = 1s

# Network parameters
network = topologies.Dumbbell
**.numberOfFlows = 5                                                    # Number of client/server pairs. Includes the "main" protocol being tested, client[0]
**.router1.ppp[0].queue.dataCapacity = QSIZE_PLACEHOLDER     # Bottleneck buffer size
**.bottleneckDatarate = BANDWIDTH_PLACEHOLDER                              # Bottleneck bandwidth
**.bottleneckDelay = DELAY_PLACEHOLDER                                      # Base RTT

# Competing cubic flow
**.server[*].tcp.typename = "TcpPaced"
**.server[*].tcp.tcpAlgorithmClass = "TcpCubic"

# Client settings
**.client[*].numApps = 1
**.client[*].app[*].typename  = "TcpGoodputSessionApp"
**.client[*].app[*].connectAddress = "server["+string(parentIndex())+"]"
**.client[0].app[*].tOpen  = 0.1s
**.client[0].app[*].tSend = 0.1s
**.client[1].app[*].tOpen  = 20s
**.client[1].app[*].tSend = 20s
**.client[*].app[*].sendBytes = 2GB

# Server settings
**.server[*].numApps = 1
**.server[*].app[*].typename = "TcpSinkApp"
**.server[*].app[*].serverThreadModuleType = "tcpgoodputapplications.applications.tcpapp.TcpGoodputSinkAppThread"

# Router/queue settings
**.router1.ppp[0].queue.typename = "DropTailQueue"                                  # Number of client/server pairs. Includes the "main" protocol being tested, client[0]

# Misc. TCP Settings
**.tcp.advertisedWindow = 200000000
**.tcp.windowScalingSupport = true
**.tcp.windowScalingFactor = -1
**.tcp.increasedIWEnabled = true
**.tcp.delayedAcksEnabled = false
**.tcp.timestampSupport = true
**.tcp.ecnWillingness = false
**.tcp.nagleEnabled = true
**.tcp.stopOperationTimeout = 4000s
**.tcp.mss = 1448
**.tcp.sackSupport = true

[Config CleanSlate]
extends = General
# "Main" flow:
**.client[0].tcp.typename = "CleanSlateTcp"
**.client[0].tcp.tcpAlgorithmClass = "CleanSlate"

# Servers
**.server[*].tcp.typename = "TcpPaced"
**.server[*].tcp.tcpAlgorithmClass = "TcpCubic"

# Orca Parameters
**.rewardDelayForgiveness = 1.25        # How much delay increases should be forgiven/ignored in reward calculation
**.rewardLossMultiplier = 5             # How strongly loss should affect reward calculation
**.printDebugMessages = false           # set to true to print debug messages for the CC algorithm
**.takeActions = true                  # If false, will stop Orca from performing actions
**.maxRLSteps = 999999            # Steps before a given agent's simulation is reset
**.fixedIntervals = true                # Interval lengths should be fixed
**.fixedIntervalDuration = .02          # Monitor interval duration in seconds. Still used as initial intervalDuration if not using fixed intervals.
debug-on-errors = true

[Config Orca]
extends = General

# A single agent uses Orca
**.client[0].tcp.typename = "OrcaTcp"
**.client[0].tcp.tcpAlgorithmClass = "Orca"

# Servers
**.server[*].tcp.typename = "TcpPaced"
**.server[*].tcp.tcpAlgorithmClass = "TcpCubic"

# Orca Parameters
**.printDebugMessages = true           # set to true to print debug messages for the CC algorithm
**.takeActions = true                  # If false, will stop Orca from performing actions
**.maxRLSteps = 999999            # Steps before a given agent's simulation is reset
**.fixedIntervals = true                # Interval lengths should be fixed
**.fixedIntervalDuration = .02          # Monitor interval duration in seconds. Still used as initial intervalDuration if not using fixed intervals.
debug-on-errors = true

[Config Cubic]
extends = Orca
**.takeActions = false

[Config Astraea]
extends = General

# Client uses Astraea
**.client[0].tcp.typename = "AstraeaTcp"
**.client[0].tcp.tcpAlgorithmClass = "Astraea"

# Servers
**.server[*].tcp.typename = "TcpPaced"
**.server[*].tcp.tcpAlgorithmClass = "TcpCubic"

# Astraea Parameters
**.rewardDelayForgiveness = 1.25        # How much delay increases should be forgiven/ignored in reward calculation
**.rewardLossMultiplier = 5             # How strongly loss should affect reward calculation
**.printDebugMessages = true             # set to true to print debug messages for the CC algorithm
**.takeActions = true                  # If false, will stop Astraea from performing actions
**.maxRLSteps = 999999                  # Steps before a given agent's simulation is reset
**.fixedIntervals = true                # Interval lengths should be fixed
**.fixedIntervalDuration = .03          # Monitor interval duration in seconds. Still used as initial intervalDuration if not using fixed intervals.
debug-on-errors = true